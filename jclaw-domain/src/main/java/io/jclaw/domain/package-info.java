/**
 * Pure functional core: the turn machine, prompt assembly, RRF fusion, redaction,
 * budget accounting, cron next-fire math.
 *
 * <p>Total, deterministic functions over immutable values. Same inputs produce
 * byte-identical transcripts — a tested invariant, not a hope.
 */
package io.jclaw.domain;

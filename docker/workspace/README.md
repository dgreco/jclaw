# jclaw's workspace

This directory is mounted at `/workspace` inside the container and is the only part of the
filesystem the agent's file and shell tools can reach. Put real work here, or point the volume
in `docker-compose.yml` somewhere else.

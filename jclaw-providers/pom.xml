<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemalocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-model-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.jclaw</groupId>
    <artifactId>jclaw-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../</relativePath>
  </parent>
  <artifactId>jclaw-providers</artifactId>
  <name>jclaw-providers</name>
  <description>ModelProvider adapters: mock (scripted golden), anthropic Messages (SSE), OpenAI-compatible, ollama, failover chain.</description>
  <dependencies>
    <dependency>
      <groupId>io.jclaw</groupId>
      <artifactId>jclaw-contracts</artifactId>
    </dependency>
    <dependency>
      <groupId>com.anthropic</groupId>
      <artifactId>anthropic-java</artifactId>
    </dependency>
    <dependency>
      <groupId>tools.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <!-- Logging facade only. The binding (logback) is contributed by jclaw-app; in tests
         SLF4J's no-op fallback applies. Keeps this module framework-free while letting it
         narrate the pipeline at DEBUG/TRACE for trace-mode runs. -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
